salario_base = float(input("Qual é o seu salário atual? "))

aumento = (salario_base * 0.15)
salario_final = (salario_base + aumento)

print(f"O seu novo salário reajustado será de R$ {salario_final}")
